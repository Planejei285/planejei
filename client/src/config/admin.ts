export const ADMIN_EMAIL = import.meta.env.VITE_ADMIN_EMAIL || "ouropretour@gmail.com";
export const ADMIN_PASSWORD = import.meta.env.VITE_ADMIN_PASSWORD || "planejei2024";

// IMPORTANTE:
// Para produção de verdade, mova essas credenciais para variáveis de ambiente no servidor.
// Em apps somente client-side, qualquer credencial embutida no front pode ser descoberta.
